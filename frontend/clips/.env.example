# Copy to `.env.local` in this folder and restart `npm run dev`.

# Required: same value as JWT_SECRET in backend/.env (middleware validates the login cookie).
JWT_SECRET=

# Optional: leave unset to use the dev proxy in next.config.ts (/clips-api → Express).
# NEXT_PUBLIC_API_URL=
